spring:
  application:
    name: Order-Service

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: user123
    driver-class-name: org.postgresql.Driver


#  datasource:
#    url: ${DB_URL}
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
#    driver-class-name: org.postgresql.Driver
#
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#
#  data:
#    redis:
#      host: ${REDIS_HOST}
#      port: ${REDIS_PORT}
#
#  liquibase:
#    change-log: classpath:db/changelog/changelog-master.yml
#    enabled: true
#
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 2000ms
          multiplier: 2.0
          max-interval: 10000ms
          max-retries: 5
#  rabbitmq:
#    host: ${RABBIT_HOST}
#    port: ${RABBIT_PORT:5672}
#    username: ${RABBIT_USERNAME}
#    password: ${RABBIT_PASSWORD}
#    listener:
#      simple:
#        retry:
#          enabled: true
#          max-attempts: 5
#          initial-interval: 2000ms
#          multiplier: 2.0
#          max-interval: 10000ms
#
saga:
    exchange: order.exchange
    order-created-key: order.created.key
#
#secret-key: ${SECRET_KEY}
#
#server:
#  port: ${SERVER_PORT:8083} # Default 8083
#
#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
#  endpoint:
#    health:
#      show-details: always
#      probes:
#        enabled: true
#  health:
#    livenessState:
#      enabled: true
#    readinessState:
server:
  port: 8080
#      enabled: true